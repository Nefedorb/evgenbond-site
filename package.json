{
  "name": "evgenbond-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-blog.mjs",
    "dev": "npm run build && http-server _site -p 4173 -c-1",
    "test": "node --test",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^16.4.0",
    "sanitize-html": "^2.17.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
